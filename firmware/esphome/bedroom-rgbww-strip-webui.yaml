packages:
  common: !include bedroom-rgbww-strip-common.yaml

esphome:
  build_path: build/bedroom-rgbww-strip-webui

web_server:
  port: 80
  auth:
    username: !secret web_username
    password: !secret web_password
